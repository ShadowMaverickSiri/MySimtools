# Visual Studio 2019 使用指南

## 📋 目录
1. [打开项目](#打开项目)
2. [配置项目](#配置项目)
3. [编译项目](#编译项目)
4. [运行程序](#运行程序)
5. [常见问题](#常见问题)

---

## 🚀 打开项目

### 步骤 1：打开解决方案

1. 启动 **Visual Studio 2019**
2. 选择 **文件 → 打开 → 项目/解决方案**
3. 导航到 `E:\MyCode\SimTools_v2\build\SimTools.sln`
4. 点击 **打开**

或者直接双击 `SimTools.sln` 文件

### 步骤 2：查看项目结构

打开后，你会在**解决方案资源管理器**中看到以下项目：

- **ALL_BUILD** - 构建所有项目
- **SimTools_example** - 示例程序
- **SimTools_static** - 静态库（核心库）
- **SimTools_test** - 测试程序
- **INSTALL** - 安装项目
- **PACKAGE** - 打包项目

---

## ⚙️ 配置项目

### 设置启动项目

1. 在**解决方案资源管理器**中，右键点击 **SimTools_test** 或 **SimTools_example**
2. 选择 **设为启动项目**

### 选择生成配置

在 VS2019 顶部工具栏：
- 配置：选择 **Release**（推荐）或 **Debug**
- 平台：选择 **x64**

> ⚠️ **重要**：确保选择 **x64** 平台，而不是 Win32

### 检查项目属性（可选）

右键点击 **SimTools_static** → **属性**：

#### C/C++ → 常规
- **附加包含目录**：应该包含 `D:\eigen3` 或类似的 Eigen3 路径

#### C/C++ → 预处理器
- **预处理器定义**：应该包含 `SIMTOOLS_STATIC;USE_EIGEN`

---

## 🔨 编译项目

### 方法 1：生成整个解决方案（推荐）

1. 在菜单栏选择 **生成 → 生成解决方案**（或按 `Ctrl+Shift+B`）
2. 等待编译完成

### 方法 2：单独编译特定项目

1. 右键点击要编译的项目（如 **SimTools_test**）
2. 选择 **生成**（或按 `Ctrl+F7`）

### 编译输出

编译成功后，在**输出**窗口应该看到：

```
生成操作已启动...
生成操作已完成...
========== 生成: 成功 8 个项目，失败 0 个项目 ==========
```

编译生成的文件位于：
- **Release**: `E:\MyCode\SimTools_v2\build\Release\`
- **Debug**: `E:\MyCode\SimTools_v2\build\Debug\`

---

## ▶️ 运行程序

### 运行测试程序

1. 确保已将 **SimTools_test** 设为启动项目
2. 按 **F5** 键（或点击 **调试 → 开始调试**）
3. 在控制台窗口查看测试结果

预期输出：
```
========================================
  SimTools v2.0 - 单元测试
========================================

Running test: Test_Math... PASSED
Running test: Test_Interpolation... PASSED
...
========================================
  所有测试通过！
========================================
```

### 运行示例程序

1. 右键点击 **SimTools_example** → **设为启动项目**
2. 按 **F5** 键运行

示例程序会展示所有模块的功能。

### 不使用调试器运行（推荐）

按 **Ctrl+F5**（或 **调试 → 开始执行（不调试）**）

---

## 📁 生成的可执行文件

编译成功后，可执行文件位于：

| 程序 | Release 位置 | Debug 位置 |
|------|--------------|-------------|
| **测试程序** | `build/Release/SimTools_test.exe` | `build/Debug/SimTools_test.exe` |
| **示例程序** | `build/Release/SimTools_example.exe` | `build/Debug/SimTools_example.exe` |
| **静态库** | `build/Release/SimTools_static.lib` | `build/Debug/SimTools_static.lib` |

你也可以在命令行中直接运行：

```cmd
cd E:\MyCode\SimTools_v2\build\Release
SimTools_test.exe
```

---

## 🔧 常见问题与解决方法

### 问题 1：找不到 Eigen3 头文件

**错误信息**：
```
fatal error C1083: 无法打开包括文件: "Eigen/Dense": No such file or directory
```

**解决方案**：

1. 确认 Eigen3 安装在 `D:\eigen3`
2. 右键项目 → **属性** → **C/C++** → **常规** → **附加包含目录**
3. 添加：`D:\eigen3`
4. 点击 **应用** 和 **确定**

### 问题 2：中文注释导致编译错误

**错误信息**：
```
error C4819: 文件包含不能在当前代码页中表示的字符
```

**解决方案**：

这个问题已经通过在 CMakeLists.txt 中添加 `/utf-8` 选项解决了。

如果仍有问题，检查项目属性：
1. 右键项目 → **属性** → **C/C++** → **命令行**
2. 确认存在 `/utf-8` 选项

### 问题 3：链接错误

**错误信息**：
```
error LNK2019: 无法解析的外部符号
```

**解决方案**：

确保按以下顺序编译：
1. 先编译 **SimTools_static**
2. 再编译 **SimTools_test** 或 **SimTools_example**

或者选择 **生成 → 生成解决方案**，让 VS 自动处理依赖关系。

### 问题 4：重载函数调用不明确

**错误信息**：
```
error C2668: "SimTools::Math::Norm2": 对重载函数的调用不明确
```

**解决方案**：

这个问题已经在代码中修复（已提交到 GitHub）。确保你的代码是最新的：

```bash
cd E:\MyCode\SimTools_v2
git pull origin main
```

然后重新生成项目：

```cmd
cd E:\MyCode\SimTools_v2\build
cmake .. -G "Visual Studio 16 2019" -A x64
```

### 问题 5：CMake 缓存问题

**症状**：修改配置后没有生效

**解决方案**：

```cmd
cd E:\MyCode\SimTools_v2
rmdir /s /q build
mkdir build
cd build
cmake .. -G "Visual Studio 16 2019" -A x64
```

---

## 📝 快速参考卡片

### 编译流程总结

```
1. 打开 SimTools.sln
   ↓
2. 选择 Release | x64
   ↓
3. 设置启动项目 (SimTools_test 或 SimTools_example)
   ↓
4. 按 Ctrl+Shift+B 编译
   ↓
5. 按 Ctrl+F5 运行
```

### 推荐配置

| 设置 | 值 | 说明 |
|------|-----|------|
| 配置 | Release | 性能更好 |
| 平台 | x64 | 64 位应用 |
| 启动项目 | SimTools_test | 用于测试 |
| 编译器 | MSVC v14.20 | VS2019 默认 |

---

## 💡 提示

### 1. 使用 IntelliSense

VS2019 的 IntelliSense 会自动补全代码，提供参数提示等。

### 2. 浏览代码

- **Ctrl+,** 转到定义
- **Shift+F12** 查找所有引用
- **F12** 转到声明

### 3. 调试

- 设置断点：按 **F9**
- 开始调试：按 **F5**
- 单步执行：**F10**（单步跳过）/ **F11**（单步进入）
- 停止调试：**Shift+F5**

### 4. 查看输出

- **输出**窗口（视图 → 输出）：查看编译信息
- **错误列表**窗口（视图 → 错误列表）：查看编译错误

---

## 🎯 完成验证

编译并运行测试后，你应该看到：

```
========================================
  SimTools v2.0 - 单元测试
========================================

Running test: Test_Math... PASSED
Running test: Test_Interpolation... PASSED
Running test: Test_Coordinate... PASSED
Running test: Test_Geodesy... PASSED
Running test: Test_Atmosphere... PASSED
Running test: Test_Random... PASSED
Running test: Test_Geometry... PASSED
Running test: Test_MatrixUtils... PASSED
Running test: Test_Units... PASSED
Running test: Test_Numerical... PASSED
Running test: Test_FileIO... PASSED
========================================
  所有测试通过！
========================================
```

看到这个输出，说明你的 VS2019 环境配置成功！🎉
